# Prototype compiler (unmodified)

`parser.py`, `graph.py`, `compiler.py`, and `main.py` are the flow-walking
BPMN-to-XState compiler of the prototype platform, copied without changes so
that the comparison in `bench_compilation.py` runs the exact code the platform
used. It requires `networkx` (`pip install networkx`).
